when performing set operations on two data sets, the
following guidelines must apply:
1. Both data sets must have the same number of columns.
2. The data types of each column across the two data sets must
be the same (or the server must be able to convert one to the
other).